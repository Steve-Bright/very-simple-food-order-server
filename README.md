THIS IS A TEST BACKEND SYSTEM FOR INNOVATION PROJECT. 

To run a project, 

1. run ```npm install ```
2. please make sure ".env" file is in the project directory, .env file contains the important information. 
3. run ```ts-node ./index.ts ```
